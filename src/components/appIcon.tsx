import React from "react";

const AppIcon = React.memo(() => {
  return (
    <span className="text-primary text-xl tracking-tight font-bold select-none">
      Theme Editor
    </span>
  );
});

export default AppIcon;
